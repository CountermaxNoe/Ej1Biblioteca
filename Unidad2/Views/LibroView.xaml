<UserControl x:Class="Unidad2.Views.LibroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Unidad2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="0,5,0,5">
        <Image Height="180" Margin="0,0,0,5" Source="{Binding Portada}"></Image>
        <TextBlock TextAlignment="Center" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" Text="{Binding Titulo}"></TextBlock>
        <TextBlock TextAlignment="Center" FontStyle="Italic" Text="{Binding Autor}"></TextBlock>
    </StackPanel>
</UserControl>
